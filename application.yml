spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: ifulfillment-config-midea-dev
    
---
spring:
  profiles: test
  application:
    name: ifulfillment-config-midea-test